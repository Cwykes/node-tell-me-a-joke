Simple node.js app with seperate api server. Tested simple node.js structure with api knowledge.

Steps:
- clone repository
- navigate to folder in terminal and run 'npm i'
- run 'api.js' then 'index.js'
- in browser navigate to 'http://localhost:3000/'
- enjoy
